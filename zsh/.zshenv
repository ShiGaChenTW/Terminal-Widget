# Minimal .zshenv for the Übersicht terminal widget.
# Disable system rc files so macOS's "default shell" notice and other noise
# never reach the widget's clean terminal surface.
unsetopt GLOBAL_RCS
